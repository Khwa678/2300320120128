# Notification Priority Inbox

Fetches notifications from API and displays top 10 notifications based on:

- Placement > Result > Event
- Most recent notifications first

Run:

npm install
node index.js